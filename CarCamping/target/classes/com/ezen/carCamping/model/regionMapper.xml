<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ezen.carCamping.model.regionMapper">
	<select id="listRegion" resultType="RegionDTO"> 
		select * from Region 
	</select>
	
	<select id="selectRegion" parameterType="Integer" resultType="RegionDTO"> <!-- xx지역 인기글 -->
		select * from Region where region_num = #{region_num}
	</select> 
	
	<select id="listCarCampingRegionHotRegion" parameterType="Integer" resultType="CarCampingRegionDTO"> <!-- 지역별 차박지 HOT차박지  -->
		select * from songsama.CarCampingRegion,Region where Region.region_num = #{region_num} and CarCampingRegion.region_num = Region.region_num and CarCampingRegion.ccr_popular = 0 order by ccr_reviewCount + ccr_likeCount + ccr_score desc limit 4
	</select>
	
	<select id="listHotRegion" resultType="CarCampingRegionDTO"> <!-- HOT 차박지 -->
		select * from songsama.CarCampingRegion where ccr_popular = 0 order by ccr_reviewCount + ccr_likeCount + ccr_score desc limit 10
	</select>
	
	<select id="listRecommandRegion" resultType="CarCampingRegionDTO"> <!-- 추천 차박지 -->
		SELECT * FROM songsama.CarCampingRegion where ccr_popular=1 and ccr_score>=4 order by ccr_score desc limit 10
	</select>
</mapper>