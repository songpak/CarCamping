<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 	"http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 
 	
<mapper namespace="com.ezen.carCamping.model.myPageMapper"> 

	<select id="cartProduct" parameterType="int" resultType="ProductCartDTO"><!-- 용품을 장바구니에 추가만 하는 쿼리문 -->
		select * from ProductCart, Product where ProductCart.prod_num=#{prod_num} and ProductCart.prod_num = Product.prod_num <!-- and ProductCart.mem_num = Member.prod_num  -->         
	</select>
	
	 <select id = "memberCart" parameterType="int" resultType="ProductCartDTO"><!-- 용품을 산 멤버의 정보를 mem_num으로 가져오기 -->
		select * from ProductCart, Member where ProductCart.prod_num=#{prod_num} and ProductCart.mem_num = Member.mem_num
	</select> 
	
</mapper>